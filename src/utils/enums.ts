export enum OS {
  Android = 'android',
  IOS = 'ios',
}

export enum PermissionStatus {
  Authorized = 'authorized',
  Denied = 'denied',
  Undefined = 'undefined',
}

export enum ContactForm {
  firstName = 'firstName',
  lastName = 'lastName',
  phoneNumber = 'phoneNumber',
  email = 'email',
}
